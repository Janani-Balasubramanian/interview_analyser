services:
  - type: web
    name: interview-analyzer-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///./interview_analyzer.db
      - key: USE_MOCK_AI
        value: "true"
      - key: OPENAI_API_KEY
        value: ""
      - key: PYTHON_VERSION
        value: "3.12.0"
