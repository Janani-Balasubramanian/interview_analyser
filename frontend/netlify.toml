[build]
  base      = "."
  publish   = "dist"
  command   = "npm run build"

[build.environment]
  NODE_VERSION = "22"

# Rewrite all routes to index.html so React Router handles them client-side
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
